# Pakete, deren Build-/Postinstall-Skripte laufen duerfen. Ohne Freigabe blockt pnpm den
# Install. esbuild braucht tsx (der Runner der Harness), die Prisma-Pakete generieren beim
# Install ihre Engine bzw. den Client.
allowBuilds:
  esbuild: true
  prisma: true
  '@prisma/client': true
  '@prisma/engines': true
